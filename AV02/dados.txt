import os
import mysql.connector

# Acesse as variáveis de ambiente
host = os.getenv("DB_HOST")
user = os.getenv("DB_USER")
password = os.getenv("DB_PASSWORD")
database = os.getenv("DB_DATABASE")

# Conecte-se ao banco de dados

conexao = mysql.connector.connect(
    host=host,
    user=user,
    passwd=password,
    database=database
)
cursor = conexao.cursor()

#CRUD
'''
INSERIR VALORES

comando = "INSERT INTO registration VALUES (%s, %s, %s, %s, %s, %s)"
cursor.execute(comando, ["12345678932", "João Jacinto Pinto", "false","2000-11-28",500,"pinto21@hotmail.com"])
conexao.commit()# para edicao
'''
# VISUALIZAR
comando =f'SELECT * FROM registration'
cursor.execute(comando)
resultado = cursor.fetchall()#para ler
print(resultado)

# ATUALIZAR
'''
comando = f'UPDATE registration SET Salary = 0 WHERE Cpf = "12345678912"'
cursor.execute(comando)
conexao.commit()
cursor.close()
conexao.close()
'''